name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: |
        cd mi-app  # Cambiar al directorio donde está tu aplicación
        npm install

    - name: Build the app
      run: |
        cd mi-app  # Cambiar al directorio donde está tu aplicación
        npm run build

    - name: Deploy to GitHub Pages
      run: |
        cd mi-app  # Cambiar al directorio donde está tu aplicación
        npm run deploy
